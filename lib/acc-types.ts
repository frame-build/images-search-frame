export type AccHub = { id: string; name: string };

export type AccProject = { id: string; name: string };

export type AccPhoto = {
  id: string;
  title: string;
  description: string;
  thumbnailUrl: string;
  fileUrl: string;
  takenAt: string;
};
